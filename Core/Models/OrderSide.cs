namespace TradingDay.Core.Models;

/// <summary>Indicates the direction of an order.</summary>
public enum OrderSide
{
    /// <summary>A buy order — acquire the asset.</summary>
    Buy,

    /// <summary>A sell order — dispose of the asset.</summary>
    Sell,
}
